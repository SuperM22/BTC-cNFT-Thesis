[package]
name = "sha_guest"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
risc0-zkvm = { version = "^3.0.3", default-features = false, features = ['std'] }
hex = "0.4"
sha-core = { path = "../../core" }
sha2 = "0.10"